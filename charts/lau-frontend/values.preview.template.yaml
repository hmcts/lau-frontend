nodejs:
  # Don't modify below here
  image: ${IMAGE_NAME}
  ingressHost: ${SERVICE_FQDN}
  environment:
    OAUTH_CLIENT_REDIRECT: https://${SERVICE_FQDN}/oauth2/callback
    HMCTS_ACCESS_ENABLED: true
    APPLICATION_INSIGHTS_NO_STATSBEAT: true
redis:
  enabled: true
